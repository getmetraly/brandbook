# Phase 08 Draft Visual Corrections

This patch updates only `/draft` related files. `/components` remains the protected baseline.

Changes:

- Enlarged pulse wave primitives across controls, state indicators and draft page separators.
- Added an explicit off switch state without pulse wave.
- Improved chart backgrounds and dark Recharts tooltip styling.
- Added heatmap tooltip styling with a visible active cell.
- Strengthened dashed drop-zone/grid lines so they are visible without strain.
- Converted widget registry status rows away from small badge-like pills.
- Made repository table rows fit inside cards by using compact grid columns and status labels.
- Added all current `getmetraly/metraly` shared icon names plus website-only icons to `/draft`.
- Added missing website patterns: marketing hero badge, CTA row, role cards, pricing cards and roadmap strip.
