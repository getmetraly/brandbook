# Visual Debt

1. Route-level screenshot comparison for `/components/previews` and the prototype-conformance pages still needs a stable browser pass at desktop and narrow widths.
2. The prototype `DesignCanvas` / `TweaksPanel` host system is still not translated into a dedicated brandbook route.
